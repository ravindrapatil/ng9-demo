import { ConvertToHmsPipe } from './convert-to-hms.pipe';

describe('ConvertToHmsPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertToHmsPipe();
    expect(pipe).toBeTruthy();
  });
});
